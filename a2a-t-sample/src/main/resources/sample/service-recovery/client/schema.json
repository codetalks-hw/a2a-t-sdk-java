{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "condition": {
      "type": "string",
      "description": "订阅条件，可选。待订阅的条件描述，例如：子网名称为xx子网。不带条件时留空。",
      "examples": [
        "子网名称：xx子网"
      ],
      "x-a2at-value-constraint": "订阅条件为子网名称。订阅可以不带条件，此时该参数为空。"
    },
    "reportFormat": {
      "type": "array",
      "description": "上报通知数据格式，必选。描述所需上报内容的字段列表，每个字段包含信息项名称（必选）、取值范围（可选）、是否必选（可选）。",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "信息项名称（必选）。"
          },
          "values": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "取值范围（可选）。"
          },
          "required": {
            "type": "boolean",
            "description": "是否必选（可选）。"
          }
        },
        "required": ["name"]
      },
      "x-a2at-value-constraint": "上报的数据类型为业务抢通事件。业务抢通事件数据包含以下字段：1. 业务抢通方案执行状态，取值范围：未启动、已结束（必选）；2. 投诉诊断任务流水号（必选）；3. OSS侧事件流水号（必选）；4. 接入端口名称（必选）；5. 是否已授权OMC自动抢通，取值范围：是、否（必选）；6. 业务抢通方案名称（必选）；7. 业务抢通方案详情（必选）；8. 业务抢通方案执行结束时间（可选）；9. 业务抢通方案执行结果，取值范围：成功、失败（可选）；10. 业务抢通方案执行失败原因，业务抢通方案执行结果为“失败”时，需要填写此信息（可选）。"
    }
  },
  "required": ["reportFormat"]
}